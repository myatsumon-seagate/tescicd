# include:
#   - project: 'myatsumon_seagate/node'
#     file: '/.gitlab-ci.yml'

stages:
  - build-node
  - build-cicd
  - scan-image

build-node-image:
  stage: build-node
  trigger:
    project: myatsumon_seagate/node
    branch: main
    strategy: depend
  only:
    - tags    
  

build-cicd-image:
  # Official docker image.
  image: docker:latest
  stage: build-cicd
  services:
    - docker:dind
  before_script:
    - docker login -u myatsumon -p $password
  script:
    - docker build -t myatsumon/testcicd .
    - docker tag myatsumon/testcicd myatsumon/testcicd:$CI_COMMIT_SHORT_SHA
    - docker push myatsumon/testcicd:$CI_COMMIT_SHORT_SHA
  only:
    - tags

scan-image:
  image: docker:19.03.1
  services:
    - docker:19.03.1-dind
  before_script:
    - docker login -u myatsumon -p $password
  stage: scan-image
  script:
    - export IMAGE_NAME=$REPO_NAME:$CI_COMMIT_SHORT_SHA
    - echo $IMAGE_NAME

    - echo "Public Registry"
    - docker login -u $REGISTRY_USER -p $REGISTRY_PASS $CI_REGISTRY

    - echo "Scan image using trivy"
    - wget https://github.com/aquasecurity/trivy/releases/download/v0.19.2/trivy_0.19.2_Linux-64bit.tar.gz
    - tar xzvf trivy_0.19.2_Linux-64bit.tar.gz
    - ./trivy $IMAGE_NAME
  only:
    - tags  

